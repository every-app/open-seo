// Marketing site origin, shared by the sitemap generator (plain Node, so it
// cannot import TypeScript) and by src/lib/brand.ts. Change it there only.
export const SITE_ORIGIN = "https://seoshark.example";
